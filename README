== Rails 2.3.8 GMail SMTP Example

This is an example app on getting a Rails 2.3.8 app working on Heroku with GMail SMTP.

=== Instructions

heroku create
git push heroku master
heroku config:add GMAIL_SMTP_USER=username
heroku config:add GMAIL_SMTP_PASSWORD=password
